---
name: gonggao-tuiwen
description: 招考/招聘公告 → 公众号推文自动化生产。输入一个公告链接（政府或事业单位网站），自动抓取公告正文、下载并解析职位表等附件，按爆款推文结构撰写 800-1500 字正式直白的推文，再做去 AI 味处理与合规自检，最后产出「参考排版版 HTML + 纯草稿 Markdown」两份文件。**内置排版引擎（vendor/gzh-design，9 套主题），开箱即用、无需外部依赖**。触发场景：(1) 用户给出招考/招聘/选调/事业单位公告链接，要求写成公众号推文；(2) 用户提到"公告转推文""职位表提取""把这个公告做成推文""考编推文"；(3) 用户给公告链接并问岗位信息、报名时间、报考条件并希望产出成稿；(4) 用户已有公告推文草稿，希望按合规/去 AI 味标准返修。适用于国省考、事业单位、医疗卫生、教师、国企等各类招考公告。不适用于纯新闻评论、产品种草等非公告类内容。
agent_created: true
---

# 公告推文生产 Skill

把一条招考公告链接，变成可直接粘贴进公众号编辑器的推文。

**核心链路**：抓取公告 → 解析职位表 → 检索考情 → 按爆款结构写稿 → 去 AI 味 → 合规自检 → 排两份版。

**两份交付物**（缺一不可）：
1. `<标题>_排版_<主题>.html` + 同名 `_预览.html` —— 带样式的参考排版版
2. `<标题>_草稿.md` —— 不套任何模板的纯 Markdown 草稿

---

## 依赖与降级

| 能力 | 依赖 | 缺失时的降级 |
|---|---|---|
| 抓取/解析/统计 | 本 skill 的 `scripts/`（零第三方依赖）| — |
| **排版** | **本 skill 内置的 `vendor/gzh-design/`**（自足，无需外部安装）| — |
| 去 AI 味 | `qu-aiwei-zh` skill（可选）| 无则按 [references/rewrite-rules.md](references/rewrite-rules.md) 人工复读改写 |
| 合规自检 | `publish-safety-officer` skill（可选）| 无则按 [references/compliance.md](references/compliance.md) 逐条自查 |
| 爆款结构 | `wechat-viral-article` skill（可选）| 无则按 [references/article-structure.md](references/article-structure.md) 执行 |

**排版引擎已内置**：`vendor/gzh-design/` 是本 skill 自带的排版库（主题组件库 + 校验/预览脚本），
**不要再去找外部 `~/.workbuddy/skills/gzh-design`，也不要凭记忆手写排版 HTML**。

下文凡出现 `{gzh}` 占位符，一律指代本 skill 的 `vendor/gzh-design` 目录。示例：

```
{gzh}/references/theme-index.md            → vendor/gzh-design/references/theme-index.md
{gzh}/scripts/validate_gzh_html.py         → vendor/gzh-design/scripts/validate_gzh_html.py
```

---

## 工作流

### 第 0 步：先问模版，选定后再开工（**强制，不得跳过**）

**用户给链接后，第一件事是用 `AskUserQuestion` 问模版，等用户选定再动第 1 步。**

不要先抓取、不要先写稿、不要"我边做边问"——模版决定排版骨架，先做再改等于返工。

**选项固定为 4 个**：自研三套 + 红白色系。**「其他模板」不预设**——用户点"其他"（或说"还有别的吗"）时，**再列出剩余 5 套**让选。

| 位置 | 选项 | 标识 | 一句话说明 | 什么时候推 |
|---|---|---|---|---|
| 1 | 公告速读蓝 | `notice-blue` | 招聘公告、考试通知，信息速览卡为核心 | 招聘/招考公告（默认推荐） |
| 2 | 考点科普绿 | `guide-green` | 考情科普、备考攻略，编号要点块做骨架 | 科普/答疑型 |
| 3 | 榜单数据橙 | `rank-amber` | 分数线、竞争比、排行榜，数据表格为主 | 数据密集型 |
| 4 | 红白色系 | `red-white` | 深度分析、观点、劝考，编辑风 | 观点/劝考型 |

把**最贴题的那套标"（推荐）"放第一位**（招聘公告类通常就是公告速读蓝）。

**用户点"其他"后，再列这 5 套：**

| 主题 | 标识 | 最适合 |
|---|---|---|
| 摸鱼绿 | `moyu-green` | 清单、工具盘点、信息速览 |
| 石墨极简风 | `graphite-minimal` | 科技评论、专业观点 |
| 留白禅意风 | `zen-whitespace` | 深度随笔、艺术留白 |
| 摸鱼票据风 | `moyu-ticket` | 测评、工具对比、创意评测 |
| 橄榄手记 | `olive-journal` | 内刊手记、深度评测、案例复盘 |

九套的完整信息（主色、适用场景、下划线色）见 `{gzh}/references/theme-index.md`。

**必须跳过提问的三种情况**（用户已经表态了，再问就是烦人）：

1. 用户在这条消息里**已经点名了模版**（"用公告速读蓝""换红白色系"）
2. 用户说**"直接排""你定""随便"**这类授权语
3. 是**续做同一篇**（改错别字、调一句话、重出预览）——模版沿用上一篇，不要重问

**顺带确认**（可与模版在同一轮问，也可用默认值直接走，视用户在意程度）：
- **字数**：800–1500 字（不含表格）
- **视角**：考编类默认**广东省视角**（省考、深圳市考、广东事业单位统考口径）。其他省份公告按公告属地口径。

### 第 1 步：抓取公告与附件

```bash
python scripts/fetch_announcement.py "<公告URL>" --out "<工作目录>"
```

产出：`公告原文.html`、`公告正文.txt`、`attachments/`、`图片清单.txt`、`清单.json`。

- **正文不足 200 字** → 页面是 JS 渲染，脚本会告警。改用 **WebFetch 工具**直接请求该 URL 提取正文，附件链接从 WebFetch 结果里取，再手工用 `parse_attachment.py` 解析。
- **页面图片多且无附件** → 职位表可能是**图片版**。加 `--download-images` 下载后，用 Read 工具逐张读图提取岗位信息。
- **要拿全附件** → 政府公告附件常在文末"相关附件"区，脚本已自动识别 `attachment/upload/0/数字` 类路径。若漏抓，用 Grep 在 `公告原文.html` 中搜 `.xlsx|.doc|.pdf` 找链接。

### 第 2 步：解析附件，提取岗位表

```bash
python scripts/parse_attachment.py "<工作目录>/attachments" 
```

产出各附件的 `*_dump.txt`。**必须完整读取职位表 dump**——岗位数量、学历分布、专业分布、招聘人数是推文的数据骨架。

读职位表时重点抓这 8 项（缺一项都别动笔）：

| 要素 | 用途 |
|---|---|
| 用人单位清单 | 开头/提要里的单位数量与名称 |
| 岗位数与总招聘人数 | 数据卡 |
| 学历门槛分布（几个岗位要研究生） | **最强筛选信号，必须单独成句** |
| 专业需求（按需求量排序） | 专业表，只列需求量大的 |
| 招聘对象/应届生类别 | 资格章 |
| 年龄口径（周岁对应的出生日期） | 资格章 |
| 服务年限、待遇 | 提要章 |
| 执业资格/证书要求 | 资格章 |

**注意**：`.doc` 老格式解析可能夹带噪声或乱序，读到明显异常时提示用户转存 `.docx`，或用 Read 工具直接读取原文件。

### 第 3 步：考情检索（招聘公告必做，别省）

公告通常只写"笔试主要测评岗位相关专业知识"这类粗口径，读者最想知道的是**考什么、怎么考**。用 WebSearch 检索该地区/该系统**历年同类批次**的笔试公告：

```
"<地区> <单位系统> 招聘 笔试公告"
"<地区> 事业单位 招聘 笔试 时长 题型"
```

要挖出来的信息：**笔试形式（线上/线下、闭卷/开卷）、单场时长、是否分公共科目、合格线、面试形式与时长、成绩公布周期**。

写进推文时**必须挂在免责提示下**，例如：

> 💡 本次笔试的具体时间和组织形式尚未公布，以上内容根据历年公告整理，最终以官方发布的笔试公告为准。

历年考情用**对比表**呈现（批次 / 形式 / 时长）。检索不到可靠历年的，**直接跳过本步**，不要编造。

### 第 4 步：写稿

读 [references/article-structure.md](references/article-structure.md) 按结构模板写，`examples/示例-成稿参考.md` 是一份真实公告的成品示例，可以先看一眼目标产出形态。严格遵守以下硬约束：

**结构主线**（读者决策链，顺序不能乱）：
> 是不是编制 → 招多少人 → **我够不够格** → 岗位在哪 → 怎么报 → 有什么坑

- **开头**：第一段只讲报名时间与报名方式。**不要写"XX单位于X月X日发布公告"**——读者不关心谁发布的。
- **三章制**：`01 公告提要` / `02 报名与考试安排` / `03 报考资格`。不要拆出"本文看点""岗位一览"这类版块。
- **数据卡**：岗位数 / 招聘人数 / 用人单位数 做成三列数据卡放 01 章开头。
- **学历门槛前置**：把"X个岗位要求研究生、仅N个放宽至本科"放在 01 章最显眼处，这是决定读者去留的信号。
- **资格信息集中**：招聘对象、学历证书要求、年龄口径、应届生类别、不得报考情形**必须全部集中在 03 章**，不得拆到流程章。
- **推图位**：不列具体岗位明细，改为占位引导。标准三处——
  - 前言下方：`领取岗位表`
  - 01 章末（专业需求表之后）：`领取岗位表`
  - 全文最末：`备考资料`
- **推图位一律指向内部**（详见下方"文案红线"），任何位置都不写"识别源文公告""去源文取"这类把人往官方原文引的表述。
- **不要写**：一句话总结式开头/结尾、END 分割线、作者签名与"点赞在看转发"三连引导（除非用户要求）。
- **标题（爆文钩子）**：写完稿后按 [references/article-structure.md](references/article-structure.md) 第七节出一组标题——**默认给 3 个备选**（1 主推 + 2 备选），每个标注钩子类型与适用场景。公式：`【地名/系统词】+【核心事实】+【反差钩子】+【时间紧迫】`，同时命中 2–3 个钩子（门槛反差 / 身份筛选 / 时间紧迫 / 数字锚点 / 稀缺反差），主标题 16–26 字。**反差必须来自公告真实事实**，禁用标题党词与绝对化用语，标题里"选聘/招聘/选调"的性质不能写错。标题同时给草稿 md 的 `#` 行和排版 HTML 的主标题卡用。

**文案红线（硬约束，不得违反）**：

1. **不引导去源文**。推图位、正文、结语、CTA 一律不提"源文公告""官方原文""去源文取"。
2. **只做内部引导**。话说给"已经在我们这里的人"听，动词用**领取 / 查收 / 自取 / 收好**，指向本号能提供的材料（岗位表、报名登记表、备考资料）。
3. **收束句的标准写法**（如"XX、XX还没下载的，赶紧领取一份。"）——主语是材料，动词是领取，落点是内部，不出现任何"去哪儿拿"的路径描述。
4. 想表达"以官方为准"这层意思时，写在**正文数据核对语境**里（如"数据以官方发布为准"），不要写成让读者跳出去找原文的指令。

**语言硬线**：
- 正式而直白，用公告原文口径（如"笔试主要测评应聘人员岗位相关专业知识"），不改写官方表述
- 禁止 AI 套话：`本文看点`、`需要提醒的是`、`值得注意的是`、`综上所述`、`总而言之`、`首先/其次/最后`、`希望能帮到你`
- 不写"一句话总结"类的概括句
- 段落不超过 4 行；同一句式不在相邻段落重复开头（尤其"本次"）
- 数据一律用表格承载，不写进长句

写完后跑字数校验：

```bash
python scripts/count_words.py "<草稿路径>" --min 800 --max 1500
```

### 第 5 步：去 AI 味

装了 `qu-aiwei-zh` 就跑检测：

```bash
python scripts/html_to_plain.py "<排版HTML>" --out html_plain.txt   # HTML 版先抽纯文本
python "<qu-aiwei-zh>/scripts/scan_ai_flavor.py" "<草稿.md>"
```

**检测结果的正确读法**——脚本的两类扣分对公告体属**误判**，不要为了提分去"修"：

| 扣分项 | 为什么不该改 |
|---|---|
| 缺乏个性化声音（无"我/我们"）| 公告转述出现第一人称反而不专业 |
| 标题 emoji 滥用 | 💡🖼📊 是排版组件图标，不是 AI 装饰 |

真正要改的是**脚本词库覆盖不到的隐性套话**：公文连接词（"需要提醒的是"）、句式重复、"本次"开头的段落堆叠、指向不明的模糊表述。详见 [references/rewrite-rules.md](references/rewrite-rules.md)。

### 第 6 步：合规自检

读 [references/compliance.md](references/compliance.md) 逐条过。招聘公告类的高危点：

- **推图位文案**：备考资料图若写"关注领取""转发后领取""扫码进群"→ 诱导关注/诱导分享，**会被限流**。
  **同时：推图位/正文不得出现"识别查看源文公告""去源文取"等把人引向官方原文的表述**——一律改为内部引导（`领取岗位表` / `领取报名登记表` / `备考资料`）。这条是硬约束，不是建议。
- **广告法绝对化用语**：最、第一、唯一、100%、包过——考编类尤其容易踩"包过"。
- **数据一致性**：岗位数、人数、时间节点必须与公告原文完全一致，逐项核对。
- **资格表述**：不得把"公告未提及"的信息写成既定条件（典型：历年公告里的加分政策，本年公告没提就别写）。

装了 `publish-safety-officer` 就调它跑一遍。

### 第 7 步：产出两份版式

**（A）参考排版版** —— 用**本 skill 内置**的排版引擎 `vendor/gzh-design/`：

1. 用**第 0 步用户已选定**的那套主题（不要在这里另选；用户跳过了提问才按第 0 步的推荐表兜底）
2. Read 该主题组件库 + `{gzh}/references/common-components.md`
   —— **组件 HTML 必须从组件库里取，不许凭记忆写**
3. 生成 HTML，推图位用通用库的**居中虚线占位组件**（🖼 + 说明文字）
4. 校验 + 生成预览页：
```bash
python "{gzh}/scripts/validate_gzh_html.py" "<排版HTML>"
python "{gzh}/scripts/wrap_preview.py" "<排版HTML>"
```
5. 校验须 **0 ERROR** 才交付；有 ERROR 按提示修完再跑一次

> 全局排版参数（正文 14px / 行高 2.5 / 段间距 2.5em / 字间距 2px）已在组件库内写死，
> 不要另行覆盖。微信铁律（`<span leaf="">` 包裹、装饰性空元素放 `<br>` 占位、
> 不给 `<strong>` 打 font-size、一个 `<p>` 一个字号）见 `{gzh}/SKILL.md`。

**（B）纯草稿版** —— Markdown，不套任何主题。推图位写成纯文本标记：`【插入推图｜领取岗位表】`。推图位文案同样是**内部引导**，不写"源文公告"。

**两版内容必须逐句一致**，改任何一版都要同步另一版。

### 第 8 步：交付

用 `present_files` 一次性交付（顺序：预览页 → 排版 HTML → 草稿 md）。文字回复里给出：

- 公告关键信息摘要表（单位/岗位数/人数/报名时间/考试方式）
- 两份文件的用途说明与粘贴方式
- **标题备选**（1 主推 + 2 备选，标注各自钩子与适用场景）——公众号发布框的标题要用户自己粘
- **需要用户自行确认的事项**：推图位待补的图、（高考情内容）以官方公告为准

---

## 环境适配（Windows + 托管 Python）

本机 bash 缺 `dirname`/`head`/`ls` 等命令，**脚本一律用托管 Python 全路径调用**：

```
C:\Users\<用户名>\.workbuddy\binaries\python\versions\<ver>\python.exe
```

- 附件下载：脚本内置 urllib，无需 curl/PowerShell
- `openpyxl` 若缺失，`parse_attachment.py` 会自动回退标准库解析（精度略降），或先 `pip install openpyxl`
- **不要用 `mcp__sheetagent__resolve_local_excel`** 处理 Windows 路径的 xlsx —— 它要求路径以 `/` 开头，Windows 下报 `INVALID_LOCAL_PATH`，直接用 Parse 脚本

更多坑见 [references/pitfalls.md](references/pitfalls.md)。

---

## 内置排版引擎（vendor/gzh-design）

排版能力已**内嵌**在本 skill 里，不需要另外安装 `gzh-design`。

```
gonggao-tuiwen/
├── SKILL.md                 ← 本文件（抓取+写稿+排版的完整流程）
├── scripts/                 ← 抓取 / 解析 / 统计
├── references/              ← 结构模板、去 AI 味、合规、踩坑
├── examples/
└── vendor/
    └── gzh-design/          ← 内置排版引擎（主题库 + 校验脚本）
        ├── SKILL.md         ← 排版引擎自身的规范（微信铁律、组件规范）
        ├── references/      ← 9 套主题组件库 + 通用组件 + 主题索引
        └── scripts/         ← validate_gzh_html.py / wrap_preview.py / component_lint.py
```

**9 套可选主题**（详见 `{gzh}/references/theme-index.md`）：

| 主题 | 标识 | 最适合 |
|---|---|---|
| 公告速读蓝 | `notice-blue` | 招聘公告、考试通知、报名提醒 |
| 红白色系 | `red-white` | 深度分析、劝考型观点 |
| 摸鱼绿 | `moyu-green` | 清单、工具盘点、信息速览 |
| 榜单数据橙 | `rank-amber` | 分数线、竞争比、排行榜单 |
| 考点科普绿 | `guide-green` | 考情科普、备考攻略、答疑 |
| 石墨极简风 | `graphite-minimal` | 科技评论、专业观点 |
| 留白禅意风 | `zen-whitespace` | 深度随笔、艺术留白 |
| 摸鱼票据风 | `moyu-ticket` | 测评、工具对比 |
| 橄榄手记 | `olive-journal` | 内刊手记、案例复盘 |

### 同步上游 gzh-design 的更新

内置版是**快照**，不会自动跟随上游。当 `~/.workbuddy/skills/gzh-design` 有更新
（新增主题、调整排版参数等）时，手动同步一次：

```powershell
$src = "C:\Users\Administrator\.workbuddy\skills\gzh-design"
$dst = "C:\Users\Administrator\.workbuddy\skills\gonggao-tuiwen\vendor\gzh-design"
Remove-Item $dst -Recurse -Force
Copy-Item $src $dst -Recurse -Force
```

注意：同步后**只同步 `references/`、`scripts/`、`SKILL.md`** 即可，
`assets/`、`docs/`、`archive/` 是上游的开发资源，对排版无影响，可不带过去。

---

## 质量自检清单（交付前逐条打勾）

- [ ] **开工前已问过模版，且用的是用户选定的那套**
- [ ] 岗位数 / 招聘人数 / 用人单位数 与职位表一致
- [ ] 所有日期时间节点与公告原文逐字核对
- [ ] 学历门槛单独成句且位于 01 章显眼处
- [ ] 资格类信息（对象/学历/年龄/应届/禁报）全在 03 章
- [ ] 三处推图位齐全，文案合规
- [ ] 正文 800–1500 字，无 AI 套话，无"一句话总结"
- [ ] 无 END 线、无签名区（除非用户要求）
- [ ] 排版 HTML 校验 0 ERROR
- [ ] 草稿版与排版版内容逐句一致
- [ ] 考情内容挂在"以官方公告为准"提示下
